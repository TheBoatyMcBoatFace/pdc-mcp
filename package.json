{
  "name": "cms-pdc-mcp",
  "version": "0.9.0",
  "private": true,
  "description": "Remote MCP server for the CMS Provider Data Catalog (data.cms.gov/provider-data)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build:dictionaries": "node scripts/build-dictionaries.mjs",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.20.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
